# Copy to .env for local overrides. On Vercel, set this in Project Settings → Environment Variables.

# Full URL of the backend API including /api (leave unset in dev — the Vite proxy handles it)
# Example: VITE_API_URL=https://your-app.up.railway.app/api
VITE_API_URL=
